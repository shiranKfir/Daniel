const path = require('path');

/**
 * webpack.config.json USE ONLY FOR AN IDE (WebStorm, VsCode, Atom) AUTOCOMPLETE
* */

module.exports = {
  resolve: {
    alias: {
      src: path.resolve(__dirname, 'src'),
    },
  },
};

/**
 * webpack.config.json USE ONLY FOR AN IDE (WebStorm, VsCode, Atom) AUTOCOMPLETE
 * */
